/*
 * This is the main function. All it does is creates a instance of
 * MayanCalculatorApplication, and runs it.
 */

int main(string[] args) {
	var app = new MayanCalculator.Application();
	return app.run();
}
